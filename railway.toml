[build]
builder = "nixpacks"

[build.buildCommand]
run = "npm install && npm run build"

[deploy]
startCommand = "node server.js"
port = 3001
```

**Yoki `Procfile` yarating:**
```
release: npm run build
web: node server.js